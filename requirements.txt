anthropic>=0.69.0
playwright>=1.48.0
httpx>=0.27.0
PyYAML>=6.0
python-dotenv>=1.0.0
edge-tts>=7.0
# Il fuso delle uscite YouTube. Su Linux zoneinfo lo prenderebbe dal sistema,
# ma se manca alza un'eccezione invece di ripiegare, e gli Short di quel
# giorno non partirebbero: costa 500 KB metterlo per iscritto.
tzdata>=2024.1
